apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: diligence-kit-web-backend-config
  namespace: prod
spec:
  healthCheck:
    checkIntervalSec: 15
    timeoutSec: 10
    healthyThreshold: 1
    unhealthyThreshold: 2
    type: HTTP
    requestPath: /
    port: 3000

