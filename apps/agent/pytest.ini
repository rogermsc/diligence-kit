[pytest]
testpaths = tests
# `python -m pytest` puts the working directory on sys.path but bare `pytest`
# does not, so without this the suites import locally and fail in CI.
pythonpath = .
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
